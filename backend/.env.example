# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (esempio per PostgreSQL o MongoDB)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=karaoke_manager
# DB_USER=your_username
# DB_PASSWORD=your_password

# MongoDB URI (se usi MongoDB)
# MONGODB_URI=mongodb://localhost:27017/karaoke_manager

# JWT Secret (IMPORTANTE: Cambia in produzione!)
JWT_SECRET=karaoke-secret-key-change-in-production

# Admin Credentials
# Username di default: admin
# Password di default: karaoke2025
# Per cambiare la password, usa l'endpoint /api/auth/change-password

# CORS Configuration
# CORS_ORIGIN=http://localhost:3000
